export const metadata = {
  title: "Login: Aarya AI",
  description: "AI tutor for all",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>;
}
